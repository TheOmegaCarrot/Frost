def n = 1800000
def input = range(n)

def squares = map input with fn (x) -> { x * x }
def filtered = filter squares with fn (x) -> { x < 1000000000 }
def total = reduce filtered init: 0 with fn (acc, x) -> { acc + x }

assert(total == 10540648931995, 'map filter reduce sum')
print(total)
