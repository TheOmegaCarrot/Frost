def n = 1400000
def combine = fn (a, b, c, d, e) -> { a + b + c + d + e }

def result = reduce range(n) init: 0 with fn (acc, i) -> { acc + combine(i, 1, 2, 3, 4) }
assert(result == 980013300000, 'closure arg bind accumulation')
print(result)
