defn fib(n) -> {
    if n < 2: n
    else: fib(n-1) + fib(n-2)
}

def input = range(30)
def fibs = map input with fib
def total = reduce fibs init: 0 with fn (acc, x) -> { acc + x }

assert(total == 1346268, 'fibonacci map reduce sum')
print(total)
