def n = 200000

def data = map range(n) with fn (i) -> {
    { a: { b: { c: { d: { e: i } } } } }
}

def result = reduce data init: 0 with fn (acc, entry) -> {
    acc + entry.a.b.c.d.e
}
assert(result == 19999900000, 'nested data traversal sum')
print(result)
