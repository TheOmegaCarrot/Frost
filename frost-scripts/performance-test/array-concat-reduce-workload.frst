def n = 13000
def chunks = map range(n) with fn (i) -> { [i, i + 1] }
def merged = reduce chunks init: [] with fn (acc, chunk) -> { acc + chunk }

def result = reduce merged init: 0 with fn (acc, x) -> { acc + x }
assert(result == 169000000, 'concat and reduce sum')
print(result)
