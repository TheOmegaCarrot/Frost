def n = 1000000

def pi_quarter = reduce range(n) init: 0.0 with fn (acc, i) -> {
    def sign = if i % 2 == 0: 1.0 else: -1.0
    acc + sign / (2.0 * i + 1.0)
}
def result = pi_quarter * 4.0

assert(result > 3.14159 and result < 3.14160, 'Leibniz pi approximation')
print(result)
