export def placeholder_name = {
    # serializes to README content
}

export def std = (
    import('encoding')
    + import('io')
)

export def ext = (
    import('sqlite')
)

export def pages = (
    import('streams')
    + import('foreign-values')
)
