# Collections

## `keys`
`keys(m)`

## `values`
`values(m)`

## `len`
`len(s)`

## `range`
`range(upper bound)`
`range(lower bound, upper bound)`

## `nulls`
`nulls(n)`

## `id`
`id(value)`

## `has`
`has(structure, index)`

## `stride`
`stride(arr, n)`

## `take`
`take(arr, n)`

## `drop`
`drop(arr, n)`

## `tail`
`tail(arr, n)`

## `drop_tail`
`drop_tail(arr, n)`

## `slide`
`slide(arr, n)`

## `chunk`
`chunk(arr, n)`

## `reverse`
`reverse(arr)`

## `take_while`
`take_while(arr, f)`

## `drop_while`
`drop_while(arr, f)`

## `chunk_by`
`chunk_by(arr, f)`

## `zip`
`zip(...arrays)`

## `xprod`
`xprod(...arrays)`

## `transform`
`transform(structure, f)`

## `select`
`select(structure, f)`

## `fold`
`fold(structure, f)`
`fold(structure, f, init)`

## `sorted`
`sorted(arr)`
`sorted(arr, f)`

## `any`
`any(arr)`
`any(arr, f)`

## `all`
`all(arr)`
`all(arr, f)`

## `none`
`none(arr)`
`none(arr, f)`

## `group_by`
`group_by(arr, f)`

## `count_by`
`count_by(arr, f)`

## `scan`
`scan(arr, f)`

## `partition`
`partition(arr, f)`
