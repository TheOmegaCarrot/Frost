def maker = fn -> {
    def outer = fn outer(arg) -> {
        def inner = fn inner_arg -> {
            outer(inner_arg)  
        }

        if arg:
            arg
        else:
            inner
    }

    outer
}

print( maker()(false)(42) )
