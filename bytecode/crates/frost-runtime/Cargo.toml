[package]
name = "frost-runtime"
version.workspace = true
edition.workspace = true

[dependencies]
frost-compile = { path = "../frost-compile" }
ryu = "1"
serde = { version = "1", features = ["derive"] }
